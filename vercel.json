{
    "buildCommand": "pip install -r requirements.txt",
    "outputDirectory": ".",
    "installCommand": "pip install -r requirements.txt",
    "framework": null,
    "routes": [
        {
            "src": "/(.*)",
            "dest": "/api/index.py"
        }
    ]
}